name: Helm Lint & Validate

on:
  pull_request:
    branches: ["main"]
    paths:
      - "deploy/helm/**"
      - ".github/workflows/helm-lint.yaml"
  push:
    branches: ["main"]
    paths:
      - "deploy/helm/**"

jobs:
  lint:
    name: Helm Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.14.4

      - name: Add required Helm repos
        run: |
          helm repo add nats https://nats-io.github.io/k8s/helm/charts
          helm repo add grafana https://grafana.github.io/helm-charts
          helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
          helm repo update

      - name: Lint all component charts
        run: |
          for chart in deploy/helm/charts/*/; do
            echo "==> Linting $chart"
            helm lint "$chart" --strict
          done

      - name: Update umbrella dependencies
        run: helm dependency update deploy/helm/cerberos

      - name: Lint umbrella chart
        run: helm lint deploy/helm/cerberos --strict -f deploy/helm/cerberos/values-dev.yaml

      - name: Template umbrella chart (dry-run)
        run: |
          helm template cerberos deploy/helm/cerberos \
            -f deploy/helm/cerberos/values-dev.yaml \
            --namespace cerberos \
            > /dev/null
          echo "Template rendered successfully."
