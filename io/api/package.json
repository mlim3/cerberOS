{
  "name": "@cerberos/io-api",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist"
  },
  "dependencies": {
    "@cerberos/io-core": "workspace:*",
    "hono": "^4.0.0",
    "nats": "^2.29.3"
  }
}
