/**
 * CLI Surface Adapter — Library Export
 *
 * Use this file when importing CLISurfaceAdapter as a library.
 * For CLI usage, run `src/cli.ts` directly.
 */

export { CLISurfaceAdapter, createCLISurface, type CLISurfaceConfig } from './surface-adapter'
